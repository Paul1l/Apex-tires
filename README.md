# APEX WHEELS

## Открыть сайт

**[Перейти в интернет-магазин APEX WHEELS](https://apex-wheels-store.apex-barnaul.workers.dev)**

Премиальная SPA-витрина интернет-магазина шин и дисков с фильтрами по размеру и
автомобилю, отдельной админкой, регистрацией, корзиной, юридическим разделом и
API-контуром обмена с 1С. География продаж и доставки ограничена Барнаулом.

## Запуск

```bash
pnpm install
pnpm dev
```

Магазин: `http://localhost:3000`  
Админка: `http://localhost:3000/admin`

Демо-администратор:

- email: `admin@apex.local`;
- пароль: `Apex2026!`.

## Статус

Это публичный интерактивный прототип, не готовый принимать реальные заказы.
Каталог, корзина и изменения админки пока сохраняются в `localStorage`
браузера. Покупательская авторизация переведена на серверный email OTP, но на
стенде она останется отключенной до подключения БД и ключей Yandex Cloud
Postbox. Файл `database/schema.sql` содержит схему, развернутой production-БД
нет.

API 1С работает в режиме `validation-only`, пока не заданы база и
`ONEC_SHARED_SECRET`. Проверить режим можно через `GET /api/1c/health`.

## Реализовано

- каталог шин и дисков, поиск, сортировка и фильтры;
- поиск по широкому справочнику марок и моделей с учетом года;
- прокручиваемые списки марок и моделей с фильтрацией по первым буквам;
- отдельный импорт точной применяемости товаров из 1С;
- избранное, сравнение и корзина;
- вход и регистрация по 6-значному коду из письма, серверная сессия;
- запрет `@gmail.com` и `@googlemail.com` на сервере и в интерфейсе;
- адаптивная админка, создание/редактирование товара, загрузка фото;
- отдельные правовые документы и согласия в формах;
- cookie-центр: Метрика не загружается до согласия;
- runtime-настройка `YANDEX_METRIKA_COUNTER_ID`;
- JSON API 1С для товаров, цен, остатков и заказов;
- идемпотентные запуски обмена и подтверждение полученных заказов;
- референсная SQL/Drizzle-схема;
- стартовая ёмкость каталога на 100 товарных позиций.

## Документация

- `docs/ARCHITECTURE.md` — модули, целевая схема и правила понятного кода;
- `docs/CODE_REFERENCE.md` — назначение основных функций и компонентов;
- `docs/DATABASE.md` — что за БД, где данные сейчас и как перейти на
  PostgreSQL в РФ;
- `docs/AUTHENTICATION.md` — email-коды, защита и подключение Postbox;
- `docs/VEHICLE_CATALOG.md` — откуда берутся автомобили и применяемость;
- `docs/ONEC_INTEGRATION.md` — подключение реальной 1С;
- `docs/LEGAL_AND_ANALYTICS.md` — реквизиты, согласия и Метрика;
- `public/1c-integration.md` — публичный контракт JSON API.

## Production checklist

1. Получить реквизиты ИП и провести юридическую проверку документов.
2. Разместить backend и PostgreSQL в РФ; применить production-миграции.
3. Перевести каталог, заказы и админку с localStorage на API.
4. Подключить email OTP к российской БД и Yandex Cloud Postbox, проверить
   rate limit и доставляемость.
5. Подключить объектное хранилище фотографий.
6. Настроить эквайринг, онлайн-кассу, доставку и уведомления.
7. Получить данные о конфигурации 1С и разработать расширение/обработку.
8. Задать секрет обмена и включить регламентные задания после staging-теста.
9. Создать счетчик Метрики и задать `YANDEX_METRIKA_COUNTER_ID`.
10. Выполнить нагрузочные, security, backup/restore и приемочные тесты.
